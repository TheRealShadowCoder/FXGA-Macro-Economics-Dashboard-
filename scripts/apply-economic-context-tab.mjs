import fs from 'node:fs';
// One-time guarded integration for the dedicated Economic Context report tab.
const path='src/App.tsx';
let source=fs.readFileSync(path,'utf8');
function replaceOne(before,after,label){const count=source.split(before).length-1;if(count!==1)throw new Error(`${label}: expected exactly one match, found ${count}`);source=source.replace(before,after);}
replaceOne("import { SimpleActionReport } from './components/SimpleActionReport';","import { SimpleActionReport } from './components/SimpleActionReport';\nimport { EconomicContextReport } from './components/EconomicContextReport';",'EconomicContextReport import');
replaceOne("type View = 'action-report' | 'overview' | 'markets' | 'analysis' | 'research' | 'signals' | 'tradingview' | 'calendar' | 'indicators' | 'universe' | 'acquisition' | 'news' | 'sources';","type View = 'action-report' | 'economic-context' | 'overview' | 'markets' | 'analysis' | 'research' | 'signals' | 'tradingview' | 'calendar' | 'indicators' | 'universe' | 'acquisition' | 'news' | 'sources';",'View union');
replaceOne("  { id: 'action-report', label: 'Action Report' },\n  { id: 'overview', label: 'Macro Dashboard' },","  { id: 'action-report', label: 'Action Report' },\n  { id: 'economic-context', label: 'Economic Context' },\n  { id: 'overview', label: 'Macro Dashboard' },",'Economic Context navigation');
replaceOne("if ((view !== 'analysis' && view !== 'action-report') || analysis || analysisLoading) return;","if ((view !== 'analysis' && view !== 'action-report' && view !== 'economic-context') || analysis || analysisLoading) return;",'Economic Context analysis loading');
replaceOne("if (view === 'action-report') { setAnalysisLoading(false); setSignalsLoading(false); setAnalysis(null); setSignals(null); void load(); }\n    else if (view === 'analysis') { setAnalysisLoading(false); setAnalysis(null); }","if (view === 'action-report') { setAnalysisLoading(false); setSignalsLoading(false); setAnalysis(null); setSignals(null); void load(); }\n    else if (view === 'economic-context') { setAnalysisLoading(false); setAnalysis(null); void load(); }\n    else if (view === 'analysis') { setAnalysisLoading(false); setAnalysis(null); }",'Economic Context refresh');
replaceOne("        {data && view === 'action-report' && <SimpleActionReport dashboard={data} analysis={analysis} signals={signals} loading={analysisLoading || signalsLoading} error={analysisError || signalsError} />}\n\n        {data && view === 'overview' && (","        {data && view === 'action-report' && <SimpleActionReport dashboard={data} analysis={analysis} signals={signals} loading={analysisLoading || signalsLoading} error={analysisError || signalsError} />}\n        {data && view === 'economic-context' && <EconomicContextReport dashboard={data} analysis={analysis} loading={analysisLoading} error={analysisError} />}\n\n        {data && view === 'overview' && (",'Economic Context render');
fs.writeFileSync(path,source);console.log('Economic Context tab integrated into App.tsx');

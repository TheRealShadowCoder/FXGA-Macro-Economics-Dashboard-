# Session kill-zone analysis

Horizon: configured high-liquidity session window.
Task: use actual session/time evidence rather than a generic clock label. Analyze liquidity, event timing, structure, volatility and whether a setup appears inside the intended window. Define entry, stop and target logic; if the window has passed, mark the setup expired.
import { Firestore } from '@google-cloud/firestore';
import { qualityScore,provenance,summarizeEvidenceQuality } from './evidence-quality.js';

const db=new Firestore({ignoreUndefinedProperties:true});
const state=db.collection('fxga_collector_state');
const marketSnapshots=db.collection('fxga_market_snapshots');
const HOUR_MS=3_600_000;
const DAY_MS=86_400_000;

const ECONOMY_BY_CURRENCY={USD:'USA',EUR:'EUROPE',GBP:'UK',ZAR:'SOUTH_AFRICA',JPY:'JAPAN'};
const FAMILY_TERMS={
  'policy-inflation':['inflation','cpi','pce','ppi','wage','earnings'],
  'policy-rate':['policy-rate','fedfunds','interest','rate','sofr','repo'],
  'labour-slack':['employment','unemployment','payroll','claims','wage','earnings'],
  'growth-activity':['growth','gdp','retail','industry','production','pmi','housing','confidence'],
  'external-balance':['trade','current-account','currency','reserves'],
};
const words=value=>String(value||'').toLowerCase().replace(/[^a-z0-9]+/g,' ').split(/\s+/).filter(Boolean);

async function readState(name){const snap=await state.doc(name).get();return snap.exists?snap.data()?.payload||null:null;}
function nearestHistory(history,eventMs){const rows=(Array.isArray(history)?history:[]).map(row=>({...row,ms:Date.parse(row?.date||'')})).filter(row=>Number.isFinite(row.ms)&&Number.isFinite(Number(row.value))).sort((a,b)=>a.ms-b.ms),prior=rows.filter(row=>row.ms<=eventMs);const current=prior.at(-1)||null,previous=prior.at(-2)||null;return current?{date:current.date,value:Number(current.value),previousDate:previous?.date||null,previous:previous?Number(previous.value):null,ageDays:Number(((eventMs-current.ms)/DAY_MS).toFixed(2))}:null;}
function scoreObservation(observation,event){const economy=ECONOMY_BY_CURRENCY[String(event?.currency||'').toUpperCase()],family=String(event?.interpretationFamily||''),terms=FAMILY_TERMS[family]||words(event?.event),text=`${observation?.seriesId||''} ${observation?.title||''} ${(observation?.categories||[]).join(' ')}`.toLowerCase();let score=0;if(economy&&((observation?.economies||[]).includes(economy)||observation?.economy===economy))score+=20;for(const term of terms)if(text.includes(term))score+=4;if(String(observation?.importance)==='critical')score+=5;return score;}
function fredSla(frequency=''){const text=String(frequency).toLowerCase();if(text.includes('daily'))return 7*DAY_MS;if(text.includes('weekly'))return 21*DAY_MS;if(text.includes('monthly'))return 70*DAY_MS;if(text.includes('quarter'))return 180*DAY_MS;return 90*DAY_MS;}

async function fredEvidence(event){const macro=await readState('macro').catch(()=>null),eventMs=Date.parse(event?.date||event?.releaseAt||'');if(!macro||!Number.isFinite(eventMs))return{status:'unavailable',source:'FRED',series:[],quality:{observations:0,scored:0,averageScore:null,states:{}}};const ranked=(macro.observations||[]).map(observation=>({observation,score:scoreObservation(observation,event)})).filter(row=>row.score>0).sort((a,b)=>b.score-a.score).slice(0,8),series=[];for(const {observation,score} of ranked){const atRelease=nearestHistory(observation.history,eventMs);if(!atRelease)continue;const evidenceQuality=qualityScore({timestamp:atRelease.date,now:eventMs,slaMs:fredSla(observation.frequency),maxAgeMs:730*DAY_MS,completeness:atRelease.previous==null?.8:1,sourceConfidence:String(observation.source||'').includes('FRED')?.98:.9,staleFallback:Boolean(observation.staleFallback)});series.push({seriesId:observation.seriesId,title:observation.title,economy:observation.economy||null,categories:observation.categories||[],units:observation.units||'',frequency:observation.frequency||'',score,atRelease,source:observation.source||'FRED',evidenceQuality,provenance:provenance({source:observation.source||'FRED',seriesId:observation.seriesId,providerTimestamp:atRelease.date,collectedAt:macro.generatedAt||null,eventId:event?.id||null,transforms:['select latest numeric observation at-or-before release timestamp','preserve previous observation when available']})});}const quality=summarizeEvidenceQuality(series);return{status:series.length?'available':'unavailable',source:'FRED',generatedAt:macro.generatedAt||null,series,quality};}

function compactMarketSnapshot(snapshot,eventMs,eventId){if(!snapshot)return null;const capturedAt=snapshot.capturedAt||null,ageMs=Number.isFinite(Date.parse(capturedAt||''))?Math.abs(eventMs-Date.parse(capturedAt)):null,assets=(snapshot.assets||[]).filter(asset=>Number.isFinite(Number(asset?.price))).map(asset=>{const source=asset.source||snapshot.source||'CNBC',evidenceQuality=qualityScore({timestamp:capturedAt,now:eventMs,slaMs:20*60_000,maxAgeMs:2*HOUR_MS,completeness:Number.isFinite(Number(asset.changePercent))?1:.85,sourceConfidence:String(source).toUpperCase().includes('CNBC')?.9:.82});return{id:asset.id,name:asset.name||asset.id,price:Number(asset.price),changePercent:Number.isFinite(Number(asset.changePercent))?Number(asset.changePercent):null,source,evidenceQuality,provenance:provenance({source,symbol:asset.id,providerTimestamp:capturedAt,collectedAt:capturedAt,eventId,transforms:['select persisted market snapshot nearest release window']})};});return{capturedAt,source:snapshot.source||'CNBC',distanceFromReleaseMs:ageMs,assets,quality:summarizeEvidenceQuality(assets)};}
async function cnbcEvidence(event){const eventMs=Date.parse(event?.date||event?.releaseAt||'');if(!Number.isFinite(eventMs))return{status:'unavailable',source:'CNBC',before:null,after:null};const from=new Date(eventMs-2*HOUR_MS).toISOString(),to=new Date(eventMs+2*HOUR_MS).toISOString();try{const query=await marketSnapshots.where('capturedAt','>=',from).where('capturedAt','<=',to).orderBy('capturedAt','asc').limit(80).get(),rows=query.docs.map(doc=>doc.data()).filter(row=>Number.isFinite(Date.parse(row?.capturedAt||''))),before=rows.filter(row=>Date.parse(row.capturedAt)<=eventMs).at(-1)||null,after=rows.find(row=>Date.parse(row.capturedAt)>=eventMs)||null,beforeCompact=compactMarketSnapshot(before,eventMs,event?.id||null),afterCompact=compactMarketSnapshot(after,eventMs,event?.id||null);return{status:before||after?'available':'unavailable',source:'CNBC persisted market snapshots',windowHours:2,before:beforeCompact,after:afterCompact,quality:{before:beforeCompact?.quality||null,after:afterCompact?.quality||null}};}catch(error){return{status:'unavailable',source:'CNBC persisted market snapshots',before:null,after:null,error:String(error?.message||error).slice(0,180)};}}

export async function buildEventEvidenceBundle(event){const[fred,cnbc]=await Promise.all([fredEvidence(event),cnbcEvidence(event)]);return{schema:'fxga.event.evidence.v2',generatedAt:new Date().toISOString(),releaseAt:event?.date||event?.releaseAt||null,authority:{release:'FXStreet/current calendar archive',pricePath:'MetaTrader 5 canonical M1',macroContext:'FRED',marketContext:'CNBC persisted snapshots'},qualityPolicy:{pointInTime:true,sourceAwareSla:true,provenanceRequired:true,syntheticMissingData:false},rules:['MT5 remains the historical price-path authority.','FRED evidence is aligned to the latest observation available on or before the release timestamp.','CNBC snapshots are context/cross-check evidence and never replace historical MT5 candles.','Quality is evaluated relative to the historical release timestamp, not today.','Missing evidence remains unavailable and is never synthesized.'],fred,cnbc};}

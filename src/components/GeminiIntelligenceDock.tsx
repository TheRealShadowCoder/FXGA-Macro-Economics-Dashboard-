import { useEffect, useMemo, useState } from 'react';
import { askFxga,getGeminiAnalysis,getGeminiHealth,getIntelligenceHealth,getPromptRegistry,type FxgaPromptTask,type GeminiAnalysis,type GeminiChat,type GeminiHealth,type GeminiMode,type IntelligenceHealth,type PromptRegistry } from '../lib/gemini-client';
import { friendlyErrorFromThrown,type FriendlyFxgaError } from '../lib/fxga-errors';
import './GeminiIntelligenceDock.css';

const LIVE_TRADE_REFRESH_MS=15_000;
const MODES:Array<{mode:Exclude<GeminiMode,'smc-signal'>;label:string;description:string}>=[
{mode:'market-brief',label:'Market',description:'Cross-asset prices and technical alignment'},
{mode:'macro-brief',label:'Macro',description:'Growth, inflation, labour, rates and conditions'},
{mode:'economic-context',label:'Economies',description:'Economic regime and cross-economy context'},
{mode:'event-research',label:'Event research',description:'Release studies, OOS validation and research maturity'},
{mode:'action-report',label:'Action report',description:'Current evidence behind WAIT / WATCH / PREPARE states'}];
const formatTime=(value?:string)=>{if(!value)return'—';const time=Date.parse(value);return Number.isFinite(time)?new Date(time).toLocaleTimeString([],{hour:'2-digit',minute:'2-digit'}):value};
function ErrorCard({value}:{value:FriendlyFxgaError}){return <div className="gemini-error-card"><strong>{value.title}</strong><p>{value.explanation}</p><small><b>What to do:</b> {value.whatToDo}</small><small><b>Retry:</b> {value.retryable?'Yes, this can usually be tried again.':'No. Fix the input/configuration or wait for the stated quota reset.'}</small>{value.retryAfterSeconds!=null&&<small><b>Retry after:</b> about {value.retryAfterSeconds} seconds</small>}<code>{value.code}{value.technical?.httpStatus?` · HTTP ${value.technical.httpStatus}`:''}</code><a href="/fxga-error-guide.html" target="_blank" rel="noreferrer">Open the full FXGA error guide ↗</a></div>}

export function GeminiIntelligenceDock(){
const[open,setOpen]=useState(false),[health,setHealth]=useState<GeminiHealth|null>(null),[intelligenceHealth,setIntelligenceHealth]=useState<IntelligenceHealth|null>(null),[registry,setRegistry]=useState<PromptRegistry|null>(null),[mode,setMode]=useState<Exclude<GeminiMode,'smc-signal'>>('action-report'),[analysis,setAnalysis]=useState<GeminiAnalysis|null>(null),[chat,setChat]=useState<GeminiChat|null>(null),[question,setQuestion]=useState(''),[task,setTask]=useState<'auto'|FxgaPromptTask>('auto'),[loading,setLoading]=useState(false),[chatLoading,setChatLoading]=useState(false),[error,setError]=useState<FriendlyFxgaError|null>(null);
useEffect(()=>{const controller=new AbortController();Promise.all([getGeminiHealth(controller.signal).catch(()=>null),getIntelligenceHealth(controller.signal).catch(()=>null),getPromptRegistry(controller.signal).catch(()=>null)]).then(([a,b,c])=>{setHealth(a);setIntelligenceHealth(b);setRegistry(c)});return()=>controller.abort()},[]);
const selected=useMemo(()=>MODES.find(item=>item.mode===mode)??MODES[0],[mode]),configured=health?.configured??intelligenceHealth?.configured??false,liveTradeTask=task!=='auto'&&String(task).endsWith('trade-management-live');
const promptGroups=useMemo(()=>{const groups=new Map<string,PromptRegistry['prompts']>();for(const item of registry?.prompts??[]){if(item.id==='live-intelligence-report')continue;const category=item.category||'other';if(!groups.has(category))groups.set(category,[]);groups.get(category)!.push(item)}return[...groups.entries()].sort(([a],[b])=>a.localeCompare(b)).map(([category,items])=>({category,items:items.sort((a,b)=>a.label.localeCompare(b.label))}))},[registry]);
async function run(nextMode=mode){setMode(nextMode);setLoading(true);setError(null);try{setAnalysis(await getGeminiAnalysis(nextMode))}catch(caught){setAnalysis(null);setError(friendlyErrorFromThrown(caught))}finally{setLoading(false)}}
async function ask(){const clean=question.trim();if(!clean)return;setChatLoading(true);setError(null);try{setChat(await askFxga(clean,task==='auto'?{}:{task}))}catch(caught){setChat(null);setError(friendlyErrorFromThrown(caught))}finally{setChatLoading(false)}}
useEffect(()=>{if(!open||!configured||!liveTradeTask||!question.trim()||chatLoading)return;const timer=window.setInterval(()=>{void ask()},LIVE_TRADE_REFRESH_MS);return()=>window.clearInterval(timer)},[open,configured,liveTradeTask,task,question,chatLoading]);
return <div className={`gemini-dock ${open?'open':''}`}><button className="gemini-dock-toggle" onClick={()=>setOpen(v=>!v)} aria-expanded={open}><span className="gemini-spark">✦</span><span>Ask FXGA AI</span><i className={configured?'online':'offline'}></i></button>{open&&<section className="gemini-dock-panel" aria-label="FXGA Gemini intelligence"><header><div><small>FXGA · Google Gemini</small><h3>Evidence Intelligence</h3></div><button onClick={()=>setOpen(false)} aria-label="Close Gemini panel">×</button></header><div className="gemini-status"><i className={configured?'online':'offline'}></i><span>{configured?`${intelligenceHealth?.model||health?.model||'Gemini'} connected · ${intelligenceHealth?.promptCount||registry?.prompts.length||0} task prompts · no FXGA hourly/daily cap`:'Gemini waiting for server configuration'}</span></div><div className="gemini-chatbox"><div className="gemini-chat-head"><strong>Ask anything about the program</strong><a href="/fxga-intelligence-live.html" target="_blank" rel="noreferrer">Open live intelligence ↗</a></div><textarea value={question} onChange={e=>setQuestion(e.target.value)} onKeyDown={e=>{if(e.key==='Enter'&&(e.ctrlKey||e.metaKey))void ask()}} placeholder="Examples: Find a scalp buy entry. Manage this day trade live. Where are the London-session target zones? Explain the long-term invalidation. Where is the strongest measured edge?"/><div className="gemini-chat-actions"><select value={task} onChange={e=>setTask(e.target.value as 'auto'|FxgaPromptTask)} aria-label="FXGA AI task"><option value="auto">Auto-select best advanced prompt</option>{promptGroups.map(group=><optgroup key={group.category} label={group.category.toUpperCase()}>{group.items.map(item=><option key={item.id} value={item.id}>{item.label}{item.realtime?' · LIVE':''}</option>)}</optgroup>)}</select><button onClick={()=>void ask()} disabled={chatLoading||!configured||!question.trim()}>{chatLoading?'Analyzing…':'Ask FXGA'}</button>{liveTradeTask&&<small className="gemini-live-note">LIVE · re-checks current evidence every 15 seconds</small>}</div>{chat&&<div className="gemini-chat-answer"><div className="gemini-output-meta"><span>{chat.label}</span><span>{chat.model}</span><span>{chat.cached?'cached':'fresh'}</span><span>{formatTime(chat.createdAt)}</span></div><div className="gemini-output-text">{chat.answer}</div><footer>Evidence: {chat.evidenceDomains.join(' · ')} · {chat.policy}</footer></div>}</div><div className="gemini-mode-title"><strong>One-click intelligence</strong><small>Specialized evidence summaries</small></div><div className="gemini-mode-grid">{MODES.map(item=><button key={item.mode} className={mode===item.mode?'active':''} onClick={()=>void run(item.mode)} disabled={loading||!configured}><strong>{item.label}</strong><small>{item.description}</small></button>)}</div>{error&&<ErrorCard value={error}/>}<div className="gemini-output">{loading&&<div className="gemini-loading"><span></span><p>Reading FXGA evidence and generating explanation…</p></div>}{!loading&&!error&&!analysis&&<div className="gemini-empty"><strong>{selected.label}</strong><p>Select a one-click intelligence mode, or ask the chatbot anything about the program. The AI receives structured FXGA evidence from Google Cloud and never receives the API key from your browser.</p></div>}{!loading&&analysis&&<><div className="gemini-output-meta"><span>{analysis.label}</span><span>{analysis.model}</span><span>{analysis.cached?'cached':'fresh'}</span><span>{formatTime(analysis.createdAt)}</span></div><div className="gemini-output-text">{analysis.output}</div><footer>{analysis.policy}</footer></>}</div></section>}</div>;
}
